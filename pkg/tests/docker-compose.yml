version: '3'

services:
  postgres-db:
    container_name: postgres-db-test
    image: postgres:13.2
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres