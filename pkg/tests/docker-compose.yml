version: '3'

services:
  pg-test:
    container_name: pg-test
    image: postgres:13.2
    ports:
      - 5433:5432
    environment:
      POSTGRES_DB: test
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres